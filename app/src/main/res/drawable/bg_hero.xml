<?xml version="1.0" encoding="utf-8"?>
<!-- Same card as bg_card, but with a signature-accent-tinted edge instead of
     the plain divider stroke — used only on the dynamic hero strip so the
     app's color identity shows up right where focus lands first. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/bg_panel_alt" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <gradient android:angle="45" android:startColor="@color/accent_soft" android:endColor="@android:color/transparent" />
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="@color/accent_dim" />
        </shape>
    </item>
</layer-list>
