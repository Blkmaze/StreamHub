<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_deep">

    <!-- Top bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="14dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/brand"
            style="@style/BrandTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_title" />

        <TextView
            android:id="@+id/sectionTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:text="@string/tab_live"
            android:textColor="@color/accent"
            android:textSize="15sp"
            android:singleLine="true" />

        <TextView
            android:id="@+id/netStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_pill"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:textColor="@color/text_primary"
            android:textSize="12sp"
            android:text="…" />

        <TextView
            android:id="@+id/clock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Broadcast notice -->
    <TextView
        android:id="@+id/noticeBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_card"
        android:padding="10dp"
        android:textColor="@color/warn"
        android:textSize="13sp"
        android:singleLine="true"
        android:ellipsize="marquee"
        android:visibility="gone" />

    <!-- Dynamic hero: reflects whatever the remote is currently focused on,
         Netflix-style — updates live as focus moves across servers / categories / channels. -->
    <LinearLayout
        android:id="@+id/heroBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="10dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_hero"
        android:padding="14dp">

        <TextView
            android:id="@+id/heroLogo"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_ribbon"
            android:gravity="center"
            android:textColor="@color/bg_deep"
            android:textSize="15sp"
            android:textStyle="bold"
            android:text="•" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="14dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/heroTitle"
                style="@style/HeroTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="19sp"
                android:singleLine="true"
                android:ellipsize="end"
                android:text="@string/hero_default_title" />

            <TextView
                android:id="@+id/heroSub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                android:singleLine="true"
                android:ellipsize="end"
                android:text="@string/hero_default_sub" />
        </LinearLayout>

        <TextView
            android:id="@+id/heroTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_pill"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:textColor="@color/accent"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Body: nav rail + 3 columns -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="8dp">

        <!-- Nav rail: wrapped in a ScrollView so every item (down through
             Settings) is reachable even on TVs/boxes with less vertical
             room — previously this was a plain LinearLayout with no way
             to scroll, so the bottom items could get clipped off-screen. -->
        <ScrollView
            android:layout_width="196dp"
            android:layout_height="match_parent"
            android:background="@drawable/bg_column"
            android:scrollbars="none"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/navRail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="6dp"
                android:paddingEnd="6dp"
                android:paddingTop="6dp"
                android:paddingBottom="6dp" />
        </ScrollView>

        <View
            android:layout_width="@dimen/col_gap"
            android:layout_height="match_parent" />

        <!-- Column 1: servers -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.05"
            android:orientation="vertical"
            android:background="@drawable/bg_column">

            <TextView
                android:id="@+id/hdrServers"
                style="@style/ColumnHeader"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/col_servers" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/listServers"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:paddingStart="6dp"
                android:paddingEnd="6dp"
                android:paddingBottom="6dp"
                android:clipToPadding="false"
                android:scrollbars="none" />
        </LinearLayout>

        <View
            android:layout_width="@dimen/col_gap"
            android:layout_height="match_parent" />

        <!-- Column 2: categories -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.15"
            android:orientation="vertical"
            android:background="@drawable/bg_column">

            <TextView
                android:id="@+id/hdrCategories"
                style="@style/ColumnHeader"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/col_categories" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/listCategories"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:paddingStart="6dp"
                android:paddingEnd="6dp"
                android:paddingBottom="6dp"
                android:clipToPadding="false"
                android:scrollbars="none" />
        </LinearLayout>

        <View
            android:layout_width="@dimen/col_gap"
            android:layout_height="match_parent" />

        <!-- Column 3: channels -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1.75"
            android:orientation="vertical"
            android:background="@drawable/bg_column">

            <TextView
                android:id="@+id/hdrChannels"
                style="@style/ColumnHeader"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/col_channels" />

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/listChannels"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:paddingStart="6dp"
                    android:paddingEnd="6dp"
                    android:paddingBottom="6dp"
                    android:clipToPadding="false"
                    android:scrollbars="none" />

                <TextView
                    android:id="@+id/emptyState"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:gravity="center"
                    android:padding="24dp"
                    android:textColor="@color/text_dim"
                    android:textSize="15sp"
                    android:visibility="gone" />

                <ProgressBar
                    android:id="@+id/progress"
                    style="?android:attr/progressBarStyle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:visibility="gone" />
            </FrameLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Key hints -->
    <TextView
        android:id="@+id/hints"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="12dp"
        android:textColor="@color/text_dim"
        android:textSize="12sp"
        android:singleLine="true"
        android:text="OK play  ·  ← → move between columns  ·  ★ favourite (long press)  ·  MENU settings" />
</LinearLayout>
